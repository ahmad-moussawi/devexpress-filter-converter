{
  "name": "devexpress-filter-converter",
  "version": "1.4.0",
  "description": "Convert the boolean tree generated by DevExpress Data Grid to a more structural format, so it can easily parsed by server side languages",
  "main": "src/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc -p .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "devexpress",
    "filter",
    "boolean-tree"
  ],
  "author": "Ahmad Moussawi <aymoussawi@gmail.com>",
  "license": "MIT"
}
